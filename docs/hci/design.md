# 校园互助平台 — 交互设计说明

> 配合人机交互课程线下展示使用。记录项目做了哪些交互设计、对应哪些课件知识点、具体代码在哪里。

## 项目概况

校园互助 Web 应用，Vue 3 + Vant 4 前端。功能覆盖：发布需求（跑腿/交易/组队/失物招领/学习互助）、浏览接单、私信聊天、双向评价、信誉评分。移动端和桌面端自适应。

---

## 一、可用性六个维度

> 课件第 3 讲：可用性目标（易学性、易记性、效用性、高效率、安全性、主观满意度）

### 1. 好学吗？（易学性 Learnability）

**做了什么**：首页每个功能入口都是「图标 + 名称 + 一句话描述」，不靠文字堆砌，一眼能看懂。表单每个输入框带图标（锁=密码、人=学号）和 placeholder 提示，不需要说明书。

**代码位置**：`Home.vue` 的 `features` 数组（第 152-158 行），Login/Register 的 `field-wrap` + `van-icon` 组合。

**理论上怎么度量**：找几个没用过的同学，统计第一次完成「发布一条需求」需要多长时间。课件提到「10 分钟法则」，我们的目标是 3 分钟内完成首次发布。

### 2. 记住了吗？（易记性 Memorability）

**做了什么**：
- 导航栏、底部按钮、发布按钮位置所有页面都一样 → 肌肉记忆
- 六种需求类型各有固定配色（跑腿=橙、交易=绿、组队=蓝、失物招领=紫、学习=红、其他=灰），列表/详情/发布页全部统一
- 「我发布的」「我接取的」tab 切换，不用去不同页面找

**课件依据**：易记性五因素——有意义的图标 ✓、固定位置 ✓、逻辑分组 ✓、通用惯例 ✓、多通道编码（颜色+文字双通道）✓。

**代码位置**：`TYPE_STYLES` 常量在 `DemandList.vue`、`DemandDetail.vue`、`MyOrders.vue` 三处定义完全相同。`main.css` 的 `--feat-1-bg` 到 `--feat-6-bg` 变量。

### 3. 功能够用吗？（效用性 Utility）

**做了什么**：从发布需求 → 浏览搜索 → 接单 → 私信沟通 → 完成确认 → 互相评价 → 信誉积分，全流程在平台内闭环，不需要切到微信或其他工具。

**课件对比**：课件举例「绘图软件只能画多边形不能徒手画 = 低效用」。我们覆盖了互助流程的全部环节。

### 4. 效率高吗？（高效率 Efficiency）

**做了什么**：
- 手机上：顶部粘着搜索+排序栏，右下角 FAB 大加号按钮一键发布，拇指刚好够到
- 电脑上：自动切换到数据表格视图，信息密度远高于卡片列表
- 首页 6 个快捷入口，一步到最常用功能

**代码位置**：`DemandList.vue` — 移动端 `van-list` 卡片流（第 68 行）+ 桌面端 `demand-table` 表格（第 36 行），通过 `@media (min-width: 768px)` 自动切换（第 293 行）。FAB 按钮（第 97 行）。

### 5. 会误操作吗？（安全性 Safety）

**做了什么**：
- 取消需求、确认接单、确认完成 — 全部弹确认框，不是一点就执行
- 退出登录弹确认框
- 提交按钮点一次后变灰转圈（`:loading`），防止重复提交
- 上传图片检查类型和大小（只允许 image/*，最大 5MB）
- 管理后台不能封禁自己

**代码位置**：`DemandDetail.vue` 的 `handleCancel`/`handleAccept`/`handleComplete`（第 398-423 行）都先调 `showConfirmDialog`。`Home.vue` 和 `Profile.vue` 退出前也弹确认。表单防重复通过 `van-button` 的 `:loading` 属性。图片校验在 `DemandPublish.vue` 的 `beforeReadImage`（第 126 行）。

### 6. 好看好用吗？（主观满意度 Satisfaction）

**做了什么**：
- 毛玻璃卡片有层次感（backdrop-filter: blur）
- 首页根据时间显示不同问候语（早上好☀️ / 夜深了🌙）
- 按钮按下去有弹性回弹（spring 曲线），不是死板的 ease
- Hero 区域紫色渐变，有温度不冷冰
- 圆角不对称（消息气泡左边圆角大右边小、详情卡片上大下小），看起来更自然

**代码位置**：毛玻璃=`main.css` 的 `.glass` 类。问候语=`Home.vue` 的 `greeting` computed。弹簧曲线=`main.css` 的 `--spring-fast-spatial` 等。

**度量方式**：可以用 QUIS 量表（课件第 6 讲，Shneiderman 开发的用户交互满意度问卷，9 级标度）做用户调查。

---

## 二、EEC 模型 — 用户完成一个操作要经历什么

> 课件第 3 讲核心框架：执行/评估活动周期（Execution/Evaluation Cycle），共 7 个阶段

拿「发布一条跑腿需求」走一遍：

### 执行阶段（用户→系统，阶段 1-4）

| 阶段 | 用户在想什么 | 我们怎么帮 |
|---|---|---|
| 1. 确立目标 | 「我要找人帮拿快递」 | 首页「跑腿代取」入口，图标+描述一眼识别 |
| 2. 形成意图 | 「点发布按钮，填表单」 | 右下角大加号 FAB，拇指范围 |
| 3. 确定动作序列 | 「选类型→写标题→填描述→标报酬→提交」 | 表单自上而下一路到底，步骤线性不绕 |
| 4. 执行动作 | 点击、输入、提交 | 按钮够大（高度 50px），字段有焦点高亮 |

### 评估阶段（系统→用户，阶段 5-7）

| 阶段 | 用户在想什么 | 系统反馈 |
|---|---|---|
| 5. 感知系统状态 | 「提交成功了吗？」 | toast 弹出「发布成功」 |
| 6. 解释系统状态 | 「成功了，跳转了」 | 自动跳转到需求列表，新需求出现在顶部 |
| 7. 评估结果 | 「嗯确实发出去了」 | 列表里能看到刚发的需求 |

### 执行隔阂 & 评估隔阂

课件说 EEC 最大的价值是帮我们发现两个「隔阂」：

- **执行隔阂**：用户想做的事 vs 系统允许做的事。比如用户想改刚发的需求，如果找不到编辑按钮就有隔阂。我们在详情页提供了编辑、取消按钮来缩小这个隔阂。
- **评估隔阂**：系统实际状态 vs 用户预期。比如点了提交按钮后页面没反应，用户不知道成功还是失败。我们用 **toast + 跳转 + 列表刷新** 三重反馈来消除这个隔阂。

---

## 三、设计规则 — 三层体系

> 课件第 3 讲：Alan Dix 基本规则 → Shneiderman 八条黄金规则 → Nielsen 十条启发式规则

### Alan Dix 三层基本规则

**可学习性**（新用户能否快速上手）：首页功能入口带图标和描述、表单有校验提示、导航结构清晰。

**灵活性**（用户能否以多种方式完成任务）：需求列表支持按类型筛选、关键词搜索、多种排序（最新/报酬高低/即将截止）、手机卡片视图和电脑表格视图。同一个目标多条路径。

**健壮性**（系统对用户的支持程度）：
- 可观察性 → 操作完立即 toast
- 可恢复性 → 需求可取消、评价可修改、封禁可解封
- 响应性 → SPA 架构无白屏等待
- 任务符合性 → 流程天然匹配校园互助场景

### 八条黄金规则逐条对照

| 规则 | 项目做了什么 | 代码位置 |
|---|---|---|
| 1. 尽可能保证一致 | 颜色/圆角/阴影/动画全抽成 CSS 变量，一处改全局生效 | `main.css` 全部 token |
| 2. 符合普遍可用性 | 手机卡片流 + 电脑表格视图，自动切换；键盘焦点可见 | `DemandList.vue` 响应式 CSS、`main.css` `:focus-visible` |
| 3. 提供信息丰富的反馈 | 加载有骨架屏、操作完有 toast、出错有 toast+重试按钮、列表为空有图标+文字 | `SkeletonLoader.vue`、各 view 的 loading/error/empty 状态 |
| 4. 设计对话框生成结束信息 | 提交成功弹 toast 并跳转，让用户明确知道「完成了」 | `DemandPublish.vue` handlePublish |
| 5. 预防并处理错误 | 表单校验、重要操作确认框、按钮防重复提交、图片类型/大小检查 | `DemandDetail.vue` confirm dialogs |
| 6. 让操作容易撤销 | 需求可取消、评价可修改、封禁可解封 | `DemandDetail.vue` handleCancel、评价编辑 |
| 7. 支持内部控制点 | 每个页面有返回按钮、用户可以主动选择下一步 | 所有页面的 `van-nav-bar left-arrow` |
| 8. 减轻短时记忆负担 | 需求类型用彩色标签+文字（不用纯文字）、留白够多不拥挤 | 所有列表页的 type badge |

### 十条启发式规则逐条对照

| 规则 | 做了什么 | 代码细节 |
|---|---|---|
| 1. 系统状态可见度 | 骨架屏加载态、toast 操作反馈、小红点未读标记、错误状态+重试按钮 | `SkeletonLoader` 闪光动画、`NavActions` 铃铛 badge |
| 2. 系统和现实世界吻合 | 全中文、学生日常词汇（「跑腿」不是「物流」）、时间显示「3 分钟前」不是时间戳 | `timeAgo()` 函数在多处使用 |
| 3. 用户控制权和自主权 | 每页有返回箭头、操作可撤销、退出要确认 | `van-nav-bar left-arrow`、confirm dialogs |
| 4. 一致性和标准化 | CSS token 体系保证全局统一 | `main.css` 200+ 行 token 定义 |
| 5. 避免出错 | 必填项校验、图片格式/大小检查、按钮防重复、重要操作确认 | forms 的 `:rules`、`beforeReadImage` |
| 6. 依赖识别而非记忆 | 类型用彩色标签+图标、状态用颜色标记（进行中=绿、取消=红灰） | `typeStyle()` 返回带颜色的 badge |
| 7. 使用的灵活性和高效性 | 新手有引导路径，老手有快捷入口（FAB、搜索）；键盘用户有 `:focus-visible` | FAB 按钮、搜索栏、68 行焦点样式 |
| 8. 审美和最小化设计 | 卡片只放必要信息、留白够多 | 每个卡片仅类型+标题+描述+发布者+时间 |
| 9. 帮助识别/诊断/恢复错误 | 所有 API 调用失败都有 toast 提示（全局拦截器），列表加载失败有重试按钮 | `client.js` 响应拦截器、列表 error state |
| 10. 帮助和文档 | 表单 placeholder 即即时帮助、字段有 icon 提示含义 | 每个输入框的 placeholder 和 icon |

---

## 四、用户体验目标 — 不只是「能用」

> 课件第 3 讲：用户体验目标 vs 可用性目标，二者可能矛盾

### 追求的具体体验

- **有质感**：毛玻璃 blur + 弹簧动画，按钮按下去有回弹感
- **有温度**：首页依时间段换问候语、Hero 用暖色渐变、头像用彩色首字母
- **令人安心**：重要操作有确认框、操作完有明确反馈、出错有恢复路径
- **不打扰**：动画有目的（反馈操作、引导注意），不是纯炫技

### 可用性 vs 体验的权衡

课件举例：游戏越难越好玩、塑料锤子砸地鼠比鼠标费劲但更有趣。我们的权衡：

- **弹簧动画多花 100-200ms**，比普通 ease-in-out「慢」，但用户感知到操作被接收了，这个感知价值 > 零点几秒
- **毛玻璃效果**在低端机上可能略卡，但校园用户设备主流性能足够

### Dark Pattern

课件提到 Dark Pattern 是指用设计诱导用户做不想做的事。本项目完全避免：确认框的取消按钮不会故意做小或藏起来、不自动勾选额外选项、信誉评分公式公开透明。

---

## 五、响应式设计 — 手机和电脑不互相迁就

> 黄金规则 #2「符合普遍可用性」+ 启发式规则 #7「灵活性和高效性」

|  | 手机 | 电脑 |
|---|---|---|
| 视图模式 | 卡片列表（手指友好） | 数据表格（眼睛友好） |
| 发布入口 | 右下角 FAB 大加号（拇指区域） | 导航栏加号图标（鼠标操作） |
| 排版 | 一列到底 | 两列 / 最大宽度居中 |
| 切换断点 | 768px 以下 | 768px 以上 |

**代码实现**：CSS `@media (min-width: 768px)` 切换。`DemandList.vue` 的 `.mobile-list` 在桌面端 `display: none`，`.table-wrap` 在手机端 `display: none`。

---

## 六、动画设计 — 每个动画都有目的

> EEC 模型评估阶段 + 黄金规则 #3「信息丰富的反馈」+ 启发式规则 #1「系统状态可见度」

| 动画 | 目的 | 时长 |
|---|---|---|
| 按钮按压回弹 | 确认操作已被接收（执行→评估桥接） | 350ms 弹簧 |
| 页面切换上移+淡入 | 空间位置感知，过渡不突兀 | 350ms emphasized |
| 骨架屏闪光 | 告知内容加载中，比白屏干等好 | 2s 循环 |
| 未读小红点脉冲 | 吸引注意但不打断 | 2s 循环 |
| Hero 装饰圆圈浮动 | 空间感，不死板 | 4.5-7s 循环 |

**核心曲线**：`cubic-bezier(0.27, 1.06, 0.18, 1.00)` — 弹簧曲线，动画末尾有 6% 过冲回弹，模拟真实弹簧手感。

---

## 七、键盘可访问性

> 启发式规则 #7「灵活性和高效性」+ 黄金规则 #2「符合普遍可用性」

新增 `main.css` 中 68 行 `:focus-visible` 样式，覆盖：
- 卡片类：获得焦点时上浮 2-3px + 紫色光环
- FAB 按钮：放大 1.08x + 发光
- 表格行：紫色背景高亮 + 内边框
- 导航项：紫色光环
- 通知/对话卡片：右移 4px + 阴影

键盘用户现在可以完整浏览和操作全部功能。

---

## 八、错误处理 — 不让用户困惑

> 启发式规则 #1「状态可见度」+ #9「帮助诊断错误」+ 黄金规则 #3「信息反馈」

### 改了什么

之前的代码有大量 `catch { /* skip */ }` — 网络出错时页面毫无反应。

**全局层**：`client.js` 的 axios 响应拦截器，所有 API 错误统一弹 toast（业务错误显示后端消息，网络错误显示「网络错误」，超时/断网显示「无法连接到服务器」）。

**视图层**（本次新增）：
- `DemandList.vue` / `MyOrders.vue`：首次加载失败显示错误图标 + 重试按钮（不用刷新页面）
- `ChatDetail.vue`：发送消息/图片上传失败显示具体提示
- `DemandPublish.vue`：图片上传失败、发布失败显示提示
- `Profile.vue`：头像上传、保存、改密码失败显示提示
- `UserList.vue`：封禁/解封操作失败显示提示

### 错误反馈的分级

参照课件第 7 讲的可用性问题分级：

| 场景 | 反馈方式 | 为什么 |
|---|---|---|
| 列表首次加载失败 | 错误图标 + 重试按钮（持久） | 用户需要知道为什么空白，需要操作出口 |
| 列表翻页加载失败 | toast（短暂） | 已有数据可见，只需提示新数据没加载 |
| 提交表单失败 | toast（短暂） | 表单还在，用户可以修改重试 |
| 发送消息失败 | toast（短暂） | 消息还在输入框，点重发即可 |
| 后台轮询失败 | 静默（不提示） | 每 10 秒一次，提示会骚扰用户 |

---

## 九、迭代设计过程

> 课件第 4 讲：形成性评估与迭代设计

从 git 提交记录看设计是怎么一步步改出来的：

1. **先做功能**（`3327e75` ~ `9f63df4`）：登录注册、需求发布接取、聊天 → 能用
2. **做响应式布局**（`d97dc2a`）：加桌面端适配 → 手机电脑都能用
3. **做美化**（`2f5f805`）：毛玻璃效果、渐变、阴影、动画 → 好看
4. **做设计系统**（`14068b6`）：颜色/圆角/阴影/动画全抽成 CSS 变量 → 好改
5. **做 HCI 细节打磨**（本次）：错误反馈、焦点可访问性、确认对话框 → 好用又安心

第三步是关键的：把设计决策变成「变量」而不是「写死的值」。评估发现问题 → 改一个变量全局生效，不用翻几十个文件。这就是课件说的 **低成本迭代**。

几个具体的迭代例子（从 git log）：
- `23b1b28`「去掉通知的全标已读按钮」— 评估发现这个按钮不常用，占空间
- `a72237b`「加表情选择器」— 聊天输入太单调
- `d97dc2a`「加桌面端适配」— 电脑看手机版浪费屏幕
- 本次提交「增强错误反馈和键盘可访问性」— 沉默失败 + 无法用键盘 = 可用性问题

---

## 十、如果用课件方法评估本项目

> 综合课件第 4-7 讲

### 形成性评估（开发中做，边做边改）

我们实际做的就是简化版形成性评估：改一版 → 自己试用 → 发现问题 → 改。课件推荐的「边做边说」（think-aloud）是最合适的下一步——找个同学，让他对着系统执行任务，嘴里说出在想什么。

### 启发式评估（找 3-5 人对照十条规则检查）

课件第 6 讲关键数据：5 名专家能发现约 80% 的可用性问题（公式 N(1-(1-L)^n)，L≈31%）。找 3-5 个同学对照十条启发式规则逐条检查，每人独立做然后汇总。

### 可用性测试（DECIDE 框架）

课件第 7 讲六步框架：

1. **确定目标**：新用户 3 分钟内能完成首次发布吗？
2. **发掘问题**：哪个步骤最困惑？哪里最慢？
3. **选择方法**：可用性测试 + 边做边说
4. **实际问题**：找 5-8 个同学，实验室环境，1 小时内
5. **道德问题**：签知情同意书，不拍脸只录屏
6. **分析数据**：统计任务完成时间、错误次数、成功率，问题分 0-4 级严重度

---

## 十一、关键设计决策一览

| 决策 | 做了什么 | 为什么 |
|---|---|---|
| CSS 变量设计系统 | 200+ 行 token 在一个文件 | 一致性（黄金#1）+ 低成本迭代 |
| 全局 axios 拦截器 | 所有 API 错误统一弹 toast | 反馈（黄金#3）+ 省代码 |
| 弹簧缓动曲线 | `cubic-bezier(0.27,1.06,0.18,1.0)` | 用户体验目标：质感 |
| 不对称圆角 | 消息气泡左边≠右边圆角 | 视觉自然，Material Design 3 特征 |
| 响应式双视图 | 手机卡片 / 电脑表格 | 普遍可用性（黄金#2） |
| 焦点可见样式 | 68 行 `:focus-visible` | 键盘可访问（启发#7） |
| 操作确认弹框 | 取消/接单/完成/退出全弹确认 | 防错（黄金#5 + 启发#5） |
| 列表错误+重试 | 加载失败显示图标+重试按钮 | 状态可见（启发#1）+ 帮助恢复（启发#9） |

---

## 十二、演示脚本 — 给老师展示什么、怎么展示

> 下面是给老师线下演示时可以走的流程。每条包含：操作路径 → 展示什么 → 对应哪个 HCI 知识点 → 可以跟老师说什么。

建议演示时长 10-15 分钟，按顺序走。如果时间紧，挑 ⭐ 标记的重点场景讲。

---

### 场景 1：登录页 — 分屏布局和焦点反馈 ⭐

**操作**：打开应用 → 自动跳转到 `/login`（未登录状态）

**展示什么**：
1. 左边紫色渐变 Hero 面板（「校园互助」logo + 标语），右边白色表单区
2. 点击学号输入框 → 边框变紫色 + 外侧光晕 + 图标变色
3. 点击密码输入框 → 同样效果
4. 缩小浏览器窗口到手机宽度 → 布局自动变成上下排列（Hero 在上、表单在下）
5. 再拉宽 → 恢复左右分屏

**HCI 知识点**：
- 焦点反馈 = 黄金规则 #3（信息丰富的反馈）— 用户清楚知道「当前在哪个输入框」
- 响应式布局 = 黄金规则 #2（符合普遍可用性）— 同一套代码适配不同设备
- 输入框带图标 = 启发式规则 #6（依赖识别而非记忆）— 锁=密码、人像=学号

**可以跟老师说**：「登录页做了响应式分屏布局，桌面端左右排列、手机端上下排列。输入框获得焦点时有 spring 过渡动画——边框变色加光晕，让用户明确感知当前焦点位置。这个设计对应 Shneiderman 黄金规则第 3 条——提供信息丰富的反馈。」

---

### 场景 2：注册页 — 表单校验和实时反馈

**操作**：点击「立即注册」→ 进入 `/register`

**展示什么**：
1. 注册表单有 4 个字段（学号、姓名、密码、确认密码），每个都有图标
2. 密码要求至少 6 位——输入 3 位就提交，会提示「密码至少 6 位」
3. 两次密码不一致就提交，会提示「两次密码不一致」——这是自定义 validator
4. 注册成功 → toast「注册成功，请登录」→ 自动跳回登录页

**HCI 知识点**：
- 表单校验 = 黄金规则 #5（预防并处理错误）— 在提交前就拦住
- 自定义密码匹配校验 = 业务逻辑层面的防错
- 成功反馈 + 自动跳转 = EEC 模型评估阶段——用户知道操作完成了

**可以跟老师说**：「注册表单做了前端校验：必填项不能为空、密码至少 6 位、两次输入必须一致。校验失败会在字段下方显示红色提示，不会等到提交到后端才报错。这对应黄金规则第 5 条——预防错误比处理错误更好。」

---

### 场景 3：首页 — 问候语、功能入口、毛玻璃效果 ⭐

**操作**：用测试账号登录 → 自动跳转到 `/`（首页）

**展示什么**：
1. **Hero 区域**：紫色渐变背景上显示「早上好☀️ / 下午好🌈 / 夜深了🌙」（根据实际时间自动切换）+ 用户名 + 「有什么需要帮忙的吗？」
2. **装饰动画**：Hero 背景有几个半透明白色圆圈在缓慢浮动（`float-slow` / `float-fast` 动画）
3. **统计栏**：毛玻璃卡片（`backdrop-filter: blur`），积分/信誉/互助次数，半透明背景能隐约看到下面 Hero 的颜色
4. **功能入口**：6 个彩色卡片（跑腿代取/二手交易/组队匹配/失物招领/学习互助/需求广场），2×3 网格，每个有图标+名称+一句话描述
5. **快捷入口**：「我的订单」卡片，点击进入
6. **底部**：「退出登录」按钮

**HCI 知识点**：
- 问候语随时间变化 = 用户体验目标——有温度、不冷冰
- 毛玻璃效果 = 主观满意度——层次感
- 6 个功能卡片 = 易学性——图标+名称+描述，三秒理解
- 装饰动画 = 审美和最小化设计——不抢眼但有空间感
- 统计栏上浮叠加 Hero = 视觉层次设计

**可以跟老师说**：「首页是用户登录后的第一印象。我们做了三件事：第一，Hero 区域根据时间段显示不同问候语，让界面有温度；第二，6 个功能入口用图标+名称+一句话描述，新用户三秒就懂；第三，统计栏用了毛玻璃效果，卡在半透明背景上，营造空间层次感。从可用性六个维度来看，这个页面覆盖了易学性、主观满意度、高效率。」

---

### 场景 4：发布需求 — 类型选择和表单交互 ⭐

**操作**：首页点击「跑腿代取」→ 进入 `/demands/publish?type=errand`

**展示什么**：
1. **类型选择器**：6 个 chip（跑腿/交易/组队/失物招领/学习/其他），当前选中的高亮为对应颜色（跑腿=橙色选中态），未选中的灰色。点击切换时有弹簧缩放动画。
2. **表单字段**：标题、描述、图片上传、地点、报酬——每个字段 focus 时边框变紫+光晕
3. **图片上传**：点击上传按钮 → 选图片 → 预览缩略图出现（圆角卡片样式）
4. **匿名开关**：打开后「匿名发布，其他人看不到你的真实姓名」
5. **提交按钮**：点击 → 按钮变灰转圈（`:loading`） → toast「发布成功」 → 自动跳转到需求列表

**HCI 知识点**：
- 类型 chip 的弹簧动画 = EEC 评估阶段——点击有回弹，感知操作被接收
- 焦点高亮 = 黄金规则 #3——知道自己在哪
- 按钮 loading 态 = 启发式规则 #1（状态可见）+ 黄金规则 #5（防重复提交）
- 成功 toast + 自动跳转 = EEC 评估阶段完整闭环

**可以跟老师说**：「发布流程的交互设计重点在反馈。类型选择器点击有弹簧动画——cubic-bezier 曲线末尾有 6% 过冲回弹，模拟真实物理手感。每个表单字段获取焦点时边框变紫加光晕，让用户明确知道当前焦点位置。提交按钮点击后立即变灰转圈，防止用户重复点击。这对应 EEC 模型的完整闭环——执行阶段的每一步都有引导，评估阶段的每一步都有反馈。」

---

### 场景 5：需求列表 — 卡片流/表格切换、搜索排序、FAB ⭐

**操作**：发布成功后自动跳转到 `/demands`（需求广场）

**展示什么**：
1. **手机宽度**：卡片列表模式——每条需求一张卡片（类型标签+标题+描述+发布者+时间），手指好点
2. **FAB 大加号按钮**：右下角紫色渐变圆按钮，有彩色阴影（`--s-primary-lg`），点击有弹簧缩放
3. **顶部工具栏**：搜索框 + 排序 chip（最新/报酬最多/报酬最少/即将截止），当前选中高亮紫色
4. **拉宽浏览器到 768px+**：卡片列表自动消失 → 变成数据表格（类型/标题/发布者/报酬/地点/时间 六列），信息密度高很多
5. **搜索**：输入关键词 → 回车 → 列表刷新
6. **排序**：点击「报酬最多」→ chip 高亮切换 → 列表重新排序

**HCI 知识点**：
- 响应式卡片↔表格 = 黄金规则 #2（普遍可用性）+ 启发式规则 #7（灵活高效）
- FAB 按钮 = 高效率——拇指区域一键发布
- 毛玻璃吸顶工具栏 = 高效率——滚动时搜索和排序始终可见
- 排序 chip 弹簧动画 = EEC 反馈
- 彩色类型标签 = 启发式规则 #6（识别优于记忆）

**可以跟老师说**：「需求列表最有代表性的设计是响应式双视图。手机上用卡片列表，手指点击友好；桌面端自动切换到数据表格，信息密度翻倍，鼠标扫读效率高。这个切换只靠 CSS 媒体查询，同一份数据、同一套逻辑。这对应黄金规则第 2 条——符合普遍可用性，也对应启发式规则第 7 条——为不同场景提供不同的高效操作方式。右下角的 FAB 大加号按钮在拇指热区范围内，彩色阴影让它从背景中跳出来。」

---

### 场景 6：需求详情 — 状态流转和确认对话框 ⭐

**操作**：点击列表中的一条需求 → 进入 `/demands/:id`

**展示什么**：
1. **详情卡片**：类型标签 + 状态标签（进行中/已接单/已完成/已取消，不同颜色）+ 标题 + 发布者信息（头像+名字+发布时间）+ 描述 + 图片 + 元数据网格（地点/报酬/截止时间）
2. **操作按钮**（非自己发布的需求）：「我要接单」大按钮
3. **点击「我要接单」** → 弹出确认框「接单后请按时完成需求，确定要接单吗？」→ 确认 → toast「接单成功」
4. **刷新后**：状态变成「已接单」，显示接单人信息
5. **发布者视角**：出现「确认完成」和「取消需求」两个按钮
6. **评价区域**：5 星评分（点击星星有缩放动画）+ 文字评价 + 提交
7. **评价可修改**：提交后显示「修改评价」按钮，点击可重新打分

**HCI 知识点**：
- 确认弹框 = 黄金规则 #5（预防错误）— 不可逆操作需要二次确认
- 状态流转 = EEC 评估阶段——状态标签颜色变化让用户感知进度
- 评价系统 = 黄金规则 #6（让操作容易撤销）— 评价可以修改
- 星评动画 = 用户体验目标——有趣
- 元数据网格 = 信息组织——一目了然

**可以跟老师说**：「需求详情页体现了状态可见性和防错设计。需求有四种状态——进行中（绿）、已接单（蓝）、已完成（灰蓝）、已取消（红灰）——每种有不同颜色标签。接单、完成、取消这些不可逆操作全都弹确认框，防止手滑。评价提交后还可以修改，给了用户反悔的空间。这对应黄金规则第 5 条预防错误、第 6 条让操作容易撤销。」

---

### 场景 7：私信聊天 — 气泡设计和表情选择器 ⭐

**操作**：在需求详情页点击「私信发布者/接单人」→ 进入 `/chat/:id`

**展示什么**：
1. **消息气泡**：自己发的在右边（紫色渐变、右下角小圆角），别人发的在左边（白色、左下角小圆角）——不对称圆角
2. **输入栏**：照片按钮 + 表情按钮 + 输入框 + 发送按钮，毛玻璃吸底
3. **表情选择器**：点击 😊 按钮 → 底部弹出 emoji picker 面板，选表情后插入光标位置
4. **发送消息**：按 Enter 或点发送 → 消息立即出现在列表底部 → 自动滚到底
5. **图片消息**：点击照片按钮 → 选图 → 上传 → 图片气泡出现 → 点击可放大预览

**HCI 知识点**：
- 不对称气泡圆角 = 视觉层次——自己的和别人的一眼区分
- 弹簧动画（发送按钮/表情 tab）= 操作反馈
- 自动滚到底 = 高效率——不需要手动滑
- 表情选择器 = 用户体验目标——表达更丰富
- Enter 快捷键 = 启发式规则 #7（灵活高效）

**可以跟老师说**：「聊天气泡用了不对称圆角设计——自己发的消息右下角是小圆角，别人发的左下角是小圆角。小细节带来大不同，用户不用看头像就能分辨消息来源。发送按钮有弹簧回弹效果，让每次发送都有『确认感』。输入栏用了毛玻璃吸底，滚动时始终可见。表情选择器插入光标位置而不是末尾，细节体验更好。」

---

### 场景 8：个人资料 — 头像上传和隐私设置

**操作**：点击导航栏右上角头像 → 进入 `/profile`

**展示什么**：
1. **Hero 区域**：紫色渐变背景 + 大头像（圆形、毛玻璃边框）+ 姓名 + 学号
2. **点击头像** → 弹出文件选择器 → 选图片 → 头像出现 loading 遮罩 → 上传完成 → toast「头像已更新」
3. **统计栏**：可用积分 / 冻结积分 / 信誉评分，毛玻璃卡片
4. **基本信息**：姓名编辑，focus 时背景变紫
5. **隐私设置**：匿名模式开关 → 打开后出现虚拟昵称输入框；关闭后输入框变灰（`:disabled` + opacity 0.4）
6. **修改密码**：旧密码 + 新密码，独立区域

**HCI 知识点**：
- 头像上传 loading 遮罩 = 启发式规则 #1（状态可见）
- 匿名开关联动 = 黄金规则 #1（一致性）— 开关状态直接影响关联字段
- 字段禁用态 = 启发式规则 #1（状态可见）— 灰色=不可用
- 退出确认弹框 = 黄金规则 #5（预防错误）

**可以跟老师说**：「个人资料页有两个值得注意的设计。一是头像上传——点击后弹出 loading 遮罩，告知用户上传进行中，上传完成后头像即时更新不需要刷新页面。二是匿名模式——开关打开后才显示虚拟昵称输入框，关闭后输入框变灰禁用，形成自然的联动。退出登录需要确认，防止误操作。」

---

### 场景 9：响应式切换 — 手机 ↔ 电脑

**操作**：在 Chrome DevTools 中拖拽视口宽度，观察布局变化

**展示什么**：
1. **768px 断点**：
   - 需求列表：卡片流 → 数据表格
   - FAB 大加号 → 消失（改为导航栏加号图标）
   - 登录/注册：上下排列 → 左右分屏
   - 首页功能网格：2 列 → 3 列
   - 个人资料：单列 → 双列
2. **桌面端 hover 效果**（手机端没有）：
   - 功能卡片 hover 时上浮 4px + 发光阴影
   - 表格行 hover 时背景变灰
   - 快捷入口卡片 hover 时上浮 2px

**HCI 知识点**：
- 响应式断点 = 黄金规则 #2（普遍可用性）
- 桌面 hover ≠ 手机 active = 针对交互模式优化
- 同一套代码 = 一致性（黄金规则 #1）

**可以跟老师说**：「整个应用的响应式设计靠 CSS 媒体查询实现，不需要两套代码。768px 是核心断点——手机用卡片触摸交互，桌面用表格鼠标交互。桌面端有 hover 效果，手机端用 active 按压效果，各取所需。」

---

### 场景 10：键盘导航 ⭐

**操作**：全程只用键盘（Tab / Shift+Tab / Enter / Esc）操作

**展示什么**：
1. 按 Tab → 焦点在各交互元素间移动，每个获得焦点的元素有紫色光环（`:focus-visible`）
2. 卡片类元素（功能卡片、快捷入口、需求卡片）→ 焦点时上浮 + 发光
3. FAB 按钮 → 焦点时放大 1.08x + 发光
4. 表格行 → 焦点时紫色背景高亮
5. 表单输入框 → 焦点时边框变紫 + 光晕（`:focus-within`）
6. 确认弹框 → 按 Esc 取消

**HCI 知识点**：
- `:focus-visible` 样式 = 启发式规则 #7（灵活高效）+ 黄金规则 #2（普遍可用性）
- 不同元素不同焦点效果 = 上下文相关的视觉反馈
- 68 行焦点样式覆盖所有交互元素

**可以跟老师说**：「我们专门为键盘用户写了 68 行 focus-visible 样式，覆盖了卡片、按钮、FAB、表格行、导航项、表单等所有交互元素。不同类型的元素有不同的焦点效果——卡片上浮发光、表格行背景高亮、FAB 放大——上下文相关的视觉反馈让键盘用户也能高效操作。这对应启发式规则第 7 条——使用的灵活性和高效性。」


### 演示路线图速查

| 顺序 | 场景 | 路由 | 重点展示 |
|---|---|---|---|
| 1 | 登录页 | `/login` | 分屏布局、焦点高亮、响应式切换 |
| 2 | 注册页 | `/register` | 表单校验、密码匹配 |
| 3 | 首页 ⭐ | `/` | 问候语、功能网格、毛玻璃、浮动动画 |
| 4 | 发布需求 ⭐ | `/demands/publish` | 类型 chip 弹簧动画、焦点反馈、loading 态 |
| 5 | 需求列表 ⭐ | `/demands` | 卡片↔表格切换、FAB、搜索排序 |
| 6 | 需求详情 ⭐ | `/demands/:id` | 状态流转、确认弹框、评价系统 |
| 7 | 私信聊天 ⭐ | `/chat/:id` | 不对称气泡、表情选择器、Enter 发送 |
| 8 | 个人资料 | `/profile` | 头像上传、匿名联动 |
| 9 | 响应式切换 | 任意页面 | 拖拽视口、hover vs active |
| 10 | 键盘导航 ⭐ | 任意页面 | Tab 走焦点、紫色光环 |

> 建议优先展示标 ⭐ 的场景（3→4→5→6→7→10），这些最直观、最有 HCI 含量。如果时间充裕再补 1、2、8、9。

---

## 十三、总结

一句话：这个项目不是随便堆了几个页面，而是 **每做一个交互决策都有 HCI 理论支撑**。

- 可用性六个维度全部覆盖
- EEC 模型贯穿交互流程
- 三层设计规则（Dix + Shneiderman + Nielsen）逐条有对应
- 用户体验超越「能用」，追求质感、温度、安心
- 错误处理从沉默失败变成明确反馈
- 键盘用户不被忽略
- 设计系统支持低成本迭代
